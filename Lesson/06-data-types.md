# পাঠ ০৬: সি ভাষায় ডেটা টাইপ (Data Types)

## সংক্ষিপ্ত বিবরণ

পাঠ ০৫-এ আমরা শিখেছি একটি ভেরিয়েবল ডিক্লেয়ার করতে গেলে তার একটি **ডেটা টাইপ** থাকতেই হবে, যেমন `int`। এই পাঠে আমরা আরও কয়েকটি গুরুত্বপূর্ণ ডেটা টাইপ শিখব — `float` আর `char` — এবং দেখব প্রতিটি টাইপের সাথে কোন **ফরম্যাট স্পেসিফায়ার** ব্যবহার করতে হয়, আর ভুল স্পেসিফায়ার ব্যবহার করলে কী সমস্যা হতে পারে।

## প্রোগ্রামটি

```c
#include <stdio.h>

int main(){
    int num1 = 45;
    float num2 = 45.45;
    char ch = 'T';
    
    printf("The number is %d\n", num1);
    
    printf("The float number is %f\n", num2);
    
    printf("The char is %c\n", ch);
    printf("%d\n", num2);
    
    return 0;
}
```

## ডেটা টাইপ কী এবং কেন দরকার?

**ডেটা টাইপ** ঠিক করে দেয়:
1. ভেরিয়েবলটি মেমোরিতে কত জায়গা (বাইট) নেবে
2. তাতে কী ধরনের ডেটা রাখা যাবে

সি একটি স্ট্যাটিক্যালি টাইপড ভাষা (পাঠ ০৫ মনে করো), তাই প্রতিটি ভেরিয়েবলের টাইপ ঘোষণার সময়েই ঠিক করে দিতে হয়, এবং সেই টাইপ পরে বদলানো যায় না।

## এই পাঠের তিনটি বেসিক ডেটা টাইপ

| ডেটা টাইপ | সাধারণত কত জায়গা নেয় | কী রাখে | উদাহরণ |
|---|---|---|---|
| `int` | ২ বা ৪ বাইট | দশমিক ছাড়া পূর্ণসংখ্যা | `45` |
| `float` | ৪ বাইট | দশমিকসহ সংখ্যা (মোটামুটি ৬-৭ ঘর দশমিক নির্ভুলতা পর্যন্ত) | `45.45` |
| `char` | ১ বাইট | একটি মাত্র ক্যারেক্টার/অক্ষর | `'T'` |

*(পরে তুমি `double`-ও শিখবে, যা `float`-এর মতোই কিন্তু বেশি জায়গা নেয় এবং অনেক বেশি নির্ভুলতা দেয় — প্রায় ১৫ ঘর দশমিক পর্যন্ত।)*

## প্রতিটি টাইপের জন্য সঠিক ফরম্যাট স্পেসিফায়ার

প্রতিটি ডেটা টাইপের নিজস্ব ফরম্যাট স্পেসিফায়ার আছে, যা `printf`-এর ভেতরে ব্যবহার করতে হয়:

| ফরম্যাট স্পেসিফায়ার | কোন টাইপের জন্য |
|---|---|
| `%d` (বা `%i`) | `int` |
| `%f` | `float` |
| `%lf` | `double` |
| `%c` | `char` |
| `%s` | স্ট্রিং/টেক্সট (পরের কোনো পাঠে শিখবে) |

**গুরুত্বপূর্ণ:** ডেটা টাইপের সাথে ঠিক ফরম্যাট স্পেসিফায়ার না মেলালে প্রোগ্রাম ভুল আউটপুট দিতে পারে, এমনকি ক্র্যাশও করতে পারে।

## কোড ব্যাখ্যা, লাইন বাই লাইন

```c
int num1 = 45;
float num2 = 45.45;
char ch = 'T';
```
তিনটি ভেরিয়েবল ডিক্লেয়ার করা হয়েছে, তিনটি আলাদা টাইপের:
- `num1` একটি `int`, মান `45`
- `num2` একটি `float`, মান `45.45`
- `ch` একটি `char`, মান `'T'` (লক্ষ্য করো, ক্যারেক্টার লেখার সময় **সিঙ্গেল কোট** `' '` ব্যবহার করতে হয়, ডাবল কোট `" "` নয়)

```c
printf("The number is %d\n", num1);
```
এখানে `%d` সঠিকভাবে `num1` (একটি `int`) এর সাথে মিলছে। আউটপুট:
```
The number is 45
```

```c
printf("The float number is %f\n", num2);
```
এখানে `%f` সঠিকভাবে `num2` (একটি `float`) এর সাথে মিলছে। আউটপুট:
```
The float number is 45.450001
```
*(কম্পাইলার ভেদে সামান্য পার্থক্য দেখা যেতে পারে, কারণ `float`-এর নির্ভুলতা সীমিত — এটা তোমার ভুল নয়, এটাই `float`-এর স্বাভাবিক আচরণ।)*

```c
printf("The char is %c\n", ch);
```
এখানে `%c` সঠিকভাবে `ch` (একটি `char`) এর সাথে মিলছে। আউটপুট:
```
The char is T
```

## ⚠️ চতুর্থ লাইনে একটি বাগ (bug) লুকিয়ে আছে

```c
printf("%d\n", num2);
```

এখানে সমস্যা আছে: `num2` একটি **`float`**, কিন্তু ব্যবহার করা হয়েছে **`%d`** — যেটা `int`-এর জন্য নির্দিষ্ট।

এর ফলাফল কী হবে তা নির্ভরযোগ্যভাবে বলা যায় না — এটাকে বলে **undefined behavior**। বাস্তবে যা ঘটতে পারে:
- একটি অর্থহীন/এলোমেলো সংখ্যা প্রিন্ট হতে পারে
- `0` প্রিন্ট হতে পারে
- কিছু কম্পাইলারে ওয়ার্নিং দেখাতে পারে, কিন্তু তারপরও কম্পাইল হয়ে যেতে পারে

**কেন এমন হয়?** `int` আর `float` মেমোরিতে সম্পূর্ণ ভিন্নভাবে সংরক্ষিত থাকে (এমনকি একই সংখ্যার জন্যও)। `%d` কম্পাইলারকে বলে "এখানে একটি `int`-এর বাইনারি প্যাটার্ন আছে ধরে নাও", কিন্তু আসলে সেখানে একটি `float`-এর বাইনারি প্যাটার্ন আছে — ফলে ভুল ব্যাখ্যা হয়।

**সঠিক লাইনটি হওয়া উচিত ছিল:**
```c
printf("%f\n", num2);
```

## মূল শিক্ষণীয় বিষয়

- `int`, `float`, এবং `char` — তিনটি বেসিক ডেটা টাইপ, যথাক্রমে পূর্ণসংখ্যা, দশমিক সংখ্যা, এবং একক ক্যারেক্টার রাখে।
- প্রতিটি ডেটা টাইপের নিজস্ব ফরম্যাট স্পেসিফায়ার আছে: `int` → `%d`, `float` → `%f`, `char` → `%c`।
- `char` মান লিখতে **সিঙ্গেল কোট** ব্যবহার করতে হয় (`'T'`), স্ট্রিং-এর জন্য ডাবল কোট (`"..."`)।
- ভুল ফরম্যাট স্পেসিফায়ার ব্যবহার করলে (যেমন `float`-এর জন্য `%d` ব্যবহার) **undefined behavior** ঘটে — আউটপুট অনির্ভরযোগ্য হয়ে যায়।
- সবসময় নিশ্চিত করো তোমার ফরম্যাট স্পেসিফায়ার ভেরিয়েবলের প্রকৃত ডেটা টাইপের সাথে ঠিকমতো মিলছে।

## সাধারণ ভুলগুলো

- `float`-এর জন্য `%d` ব্যবহার করা (এই লেসনে যেমন দেখানো হয়েছে)।
- `char`-এর মান লিখতে ডাবল কোট ব্যবহার করা (`"T"` — এটা একটা স্ট্রিং হয়ে যায়, char নয়)।
- `double`-এর জন্য `%f`-এর বদলে `%lf` ব্যবহার করতে ভুলে যাওয়া (যদিও অনেক আধুনিক কম্পাইলারে `printf`-এ এটা কাজ করে, তবু সঠিক অভ্যাস গড়া ভালো)।

## অনুশীলনী

1. উপরের প্রোগ্রামের বাগ ঠিক করো — `printf("%d\n", num2);` লাইনটি সঠিক ফরম্যাট স্পেসিফায়ার দিয়ে ঠিক করো।
2. একটি নতুন `double` ভেরিয়েবল ডিক্লেয়ার করো, মান `3.14159265`, এবং `%lf` ব্যবহার করে প্রিন্ট করো।
3. ইচ্ছাকৃতভাবে `int`-এর জন্য `%c` ব্যবহার করে দেখো কী প্রিন্ট হয় — ফলাফলটা কি তোমার প্রত্যাশা মতো হলো?

---
*মজা করো, রিস্ক নাও, পরের পাঠে দেখা হচ্ছে! 🚀*
