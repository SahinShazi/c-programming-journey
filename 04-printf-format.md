# পাঠ ০৪: আরও `printf` — এস্কেপ সিকোয়েন্স, ফরম্যাট স্পেসিফায়ার ও গাণিতিক হিসাব

## সংক্ষিপ্ত বিবরণ

`printf` সম্পর্কে বেসিক ধারণা তো হয়ে গেছে। এই পাঠে আমরা আরেকটু গভীরে যাব:
- একই প্রোগ্রামে একাধিক `printf` ব্যবহার
- নতুন একটি এস্কেপ সিকোয়েন্স: `\t` (ট্যাব)
- `%d` ব্যবহার করে **গাণিতিক হিসাবের ফলাফল** প্রিন্ট করা

## প্রোগ্রামটি

```c
#include <stdio.h>

int main(){
    printf("My name is Sahin.\n");
    printf("I have a channel.\n");
    printf("I want \t to learn C for A good job\n");

    printf("%d\n", 6+7);
    printf("%d\n", 88-99);
    printf("%d\n", 99999 / 99);
    
    return 0;
}
```

## লাইন বাই লাইন ব্যাখ্যা

### `printf("My name is Sahin.\n");` এবং `printf("I have a channel.\n");`
এগুলো সাধারণ টেক্সট প্রিন্ট, প্রতিটির শেষে `\n` আছে যাতে পরবর্তী `printf` নতুন লাইনে শুরু হয়। এই প্যাটার্ন তুমি পাঠ ০৩-এ দেখেছ।

### `printf("I want \t to learn C for A good job\n");`
এখানে একটি নতুন এস্কেপ সিকোয়েন্স আছে: **`\t`**

- `\t` মানে হলো একটি **হরাইজন্টাল ট্যাব** (তোমার টার্মিনালের উপর নির্ভর করে এটি সাধারণত কয়েকটি স্পেসের সমান হয়)।
- এটি টেক্সটকে সুন্দরভাবে কলামে সাজাতে কাজে লাগে।

এখন পর্যন্ত তুমি যে এস্কেপ সিকোয়েন্সগুলো জানো:
| সিকোয়েন্স | অর্থ |
|---|---|
| `\n` | নতুন লাইন |
| `\t` | ট্যাব (হরাইজন্টাল স্পেস) |

### `printf("%d\n", 6+7);`
এই লাইনে আসছে **ফরম্যাট স্পেসিফায়ার** — সি প্রোগ্রামিং-এর একটি গুরুত্বপূর্ণ ধারণা।

- `%d` একটি **প্লেসহোল্ডার**, যা `printf`-কে বলে দেয় "এখানে একটি ইন্টিজার (পূর্ণসংখ্যা) বসাও।"
- `6+7` প্রথমে হিসাব হয় (সি স্বাভাবিক গাণিতিক নিয়ম মেনে চলে), যার ফলাফল হয় `13`।
- সেই ফলাফল (`13`) `%d` জায়গায় বসে যায়।
- তাই এই লাইনের আউটপুট হবে: `13`

### `printf("%d\n", 88-99);`
একই নিয়ম, তবে এবার বিয়োগ।
- `88 - 99` এর ফলাফল হয় `-11`।
- আউটপুট: `-11`

এটি দেখায় যে `%d` দিয়ে **ঋণাত্মক সংখ্যাও** ঠিকভাবে প্রিন্ট করা যায়।

### `printf("%d\n", 99999 / 99);`
এই লাইনে আছে **ইন্টিজার ডিভিশন (পূর্ণসংখ্যা ভাগ)**।

- গাণিতিকভাবে `99999 / 99` = `1010.09...`
- কিন্তু যেহেতু `99999` এবং `99` দুটোই **ইন্টিজার** (দশমিক ছাড়া পূর্ণসংখ্যা), তাই সি ভাষা এখানে **ইন্টিজার ডিভিশন** করে, যা দশমিকের পরের অংশ সম্পূর্ণ **বাদ দিয়ে দেয়** (রাউন্ড করে না)।
- ফলাফল: `1010`

এটি সি-এর একটি গুরুত্বপূর্ণ বৈশিষ্ট্য: `/` দিয়ে দুটি ইন্টিজার ভাগ করলে সবসময় ইন্টিজার ফলাফল পাওয়া যায় — দশমিক অংশ রাউন্ড না হয়ে ট্রাংকেট (কেটে ফেলা) হয়।

## প্রত্যাশিত আউটপুট

```
My name is Sahin.
I have a channel.
I want 	 to learn C for A good job
13
-11
1010
```

## মূল শিক্ষণীয় বিষয়

- `\t` আউটপুটে একটি ট্যাব স্পেস যোগ করে — ফরম্যাটিং/অ্যালাইনমেন্টে কাজে লাগে।
- `%d` হলো একটি **ফরম্যাট স্পেসিফায়ার**, যা `printf`-এর ভেতরে ইন্টিজার ভ্যালু প্রিন্ট করতে ব্যবহৃত হয়।
- তুমি সরাসরি একটি **এক্সপ্রেশন** (যেমন `6+7`) প্রিন্ট করার জন্য বসাতে পারো — সি প্রথমে সেটি হিসাব করে, তারপর ফলাফল প্রিন্ট করে।
- সি-তে দুটি ইন্টিজার ভাগ করলে **ইন্টিজার ডিভিশন** হয়: দশমিক অংশ রাউন্ড না হয়ে বাদ পড়ে যায়। `99999 / 99` এর ফলাফল `1010` হবে, `1010.09` নয়।

## সাধারণ ভুলগুলো

- ইন্টিজার নয় এমন কোনো মান (যেমন `float`) প্রিন্ট করতে `%d` ব্যবহার করা — এতে ভুল আউটপুট আসবে। পরের কোনো পাঠে দশমিক সংখ্যার সঠিক স্পেসিফায়ার শেখানো হবে।
- ধরে নেওয়া যে দুটি ইন্টিজারের মধ্যে `/` ব্যবহার করলে দশমিক উত্তর পাওয়া যাবে — যাবে না, যদি না কোনো একপাশে ফ্লোটিং-পয়েন্ট সংখ্যা থাকে।
- `printf("%d\n", value);`-এ ফরম্যাট স্ট্রিং এবং ভ্যালুর মাঝে কমা দিতে ভুলে যাওয়া।

## অনুশীলনী

আরও তিনটি `printf` স্টেটমেন্ট যোগ করো `%d` ব্যবহার করে, যা প্রিন্ট করবে:
1. `12 * 4` এর ফলাফল
2. `50 / 7` এর ফলাফল (প্রোগ্রাম চালানোর আগেই ভেবে দেখো ইন্টিজার ডিভিশনে কী ফলাফল আসবে!)
3. `50 / 7`-এর ভাগশেষ, `%` (মডুলাস) অপারেটর ব্যবহার করে — চেষ্টা করো `printf("%d\n", 50 % 7);` লিখে দেখতে কী প্রিন্ট হয়।

---
*মজা করো, রিস্ক নাও, পরের পাঠে দেখা হচ্ছে! 🚀*
